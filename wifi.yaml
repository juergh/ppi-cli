network:
    wifis:
        wlan0:
            optional: true
            dhcp4: true
            access-points:
                "${SSID}":
                    password: "${PASSPHRASE}"
    version: 2
